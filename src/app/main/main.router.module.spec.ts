import { Main.Router.Module } from './main.router.module';

describe('Main.Router.Module', () => {
  it('should create an instance', () => {
    expect(new Main.Router.Module()).toBeTruthy();
  });
});
